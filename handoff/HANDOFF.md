# Manager handoff (Claude Code)

This folder is the **design reference** your manager built with Claude Code — not production code to ship as-is.

| Path | Contents |
|------|----------|
| `app/` | ~30 JSX screen modules (booking, cart, calendar, CRM, shop, …) |
| `Barber Booking.html` | Browser prototype entry (React 18 + Babel) |
| `assets/`, `screenshots/` | Brand assets and UI captures |
| `design_handoff_barber_app/` | Duplicate handoff bundle + original README |

**Task:** Re-implement these designs in `beta/apps/BarberShop/front/` (React Native / Expo) using real navigation, API, and state — see the full spec in the README below.

---
