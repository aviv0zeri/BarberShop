# mobile-api (planned)

REST API for BarberShop mobile app. **Not implemented in v2.0.0.**

When ready:

1. Implement app in `beta/api/mobile-api/`
2. Add `Dockerfile` + `compose.yml` here (see GateOpen `docker/mobile-api/`)
3. Default port: `8090` (`BETA_API_PORT`)

```bash
# future
scripts/local-dev.sh beta-api
```
